/**
 * Created by jonirrings on 17/4/22.
 */
import { describe, it } from 'mocha';
import { expect } from 'chai';

describe('just test', () => {
  it('just to past the travis ci', () => {
    expect('ok').to.equal('ok');
  });
});
